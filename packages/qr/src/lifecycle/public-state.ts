/**
 * VaahanSafe Public Resolver State Mapping (Canonical Lifecycle Re-export)
 *
 * SECTION 32 — INTERNAL VS PUBLIC STATES:
 * Maps internal operations/inventory lifecycle states to safe public resolver states.
 * Single source of truth is maintained in @vaahansafe/qr-core/resolver.
 */

export * from "../resolver/types";
export * from "../resolver/states";
